apiVersion: v1
kind: Service
metadata:
  name: weather-dashboard-service
spec:
  selector:
    app: weather-dashboard
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8050
  type: NodePort
